doctype html 
html(lang="ja")
  head
    meta(charset="utf-8")
    link(href="/css/app.css" rel="stylesheet" type="text/css")
  body
    include header.pug
    section(id="main")
      h1 メッセージを保存
      form(action="update" method="POST" encType="multipart/form-data")
        input(type="hidden" name="_csrf" value=csrf)
        textarea(name="message" placeholder="メッセージ" id="textarea_message")
        input(type="file" name="image" id="input_file")
        button(type="submit" id="submit_button") 送信